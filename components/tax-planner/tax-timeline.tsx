import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"

export function TaxTimeline() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Tax Timeline</CardTitle>
        <CardDescription>Important dates and preparations</CardDescription>
      </CardHeader>
      <CardContent>{/* Add timeline content here */}</CardContent>
    </Card>
  )
}

